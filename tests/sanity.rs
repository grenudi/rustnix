//! Integration tests live here, one file per concern, following argenv's
//! convention (crates/argenv/tests/*.rs): each file gets its own module of
//! related tests rather than one large tests.rs. This file is a placeholder -
//! replace it with real coverage once the crate does something.

#[test]
fn placeholder() {
    assert_eq!(2 + 2, 4);
}
